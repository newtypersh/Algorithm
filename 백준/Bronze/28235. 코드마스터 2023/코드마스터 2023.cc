#include <iostream>

using namespace std;

int main() {
	string str;
	cin >> str;
	if (str == "SONGDO") {
		cout << "HIGHSCHOOL";
	}
	if (str == "CODE") {
		cout << "MASTER";
	}
	if (str == "2023") {
		cout << "0611";
	}
	if (str == "ALGORITHM") {
		cout << "CONTEST";
	}

}